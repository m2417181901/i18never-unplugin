'use strict';

var parser = require('@babel/parser');
var generate = require('@babel/generator');
var t = require('@babel/types');
var traverse = require('@babel/traverse');
var shared = require('@i18never/shared');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

function _interopNamespaceCompat(e) {
    if (e && typeof e === 'object' && 'default' in e) return e;
    var n = Object.create(null);
    if (e) {
        Object.keys(e).forEach(function (k) {
            if (k !== 'default') {
                var d = Object.getOwnPropertyDescriptor(e, k);
                Object.defineProperty(n, k, d.get ? d : {
                    enumerable: true,
                    get: function () { return e[k]; }
                });
            }
        });
    }
    n.default = e;
    return Object.freeze(n);
}

var generate__default = /*#__PURE__*/_interopDefaultCompat(generate);
var t__namespace = /*#__PURE__*/_interopNamespaceCompat(t);
var traverse__default = /*#__PURE__*/_interopDefaultCompat(traverse);

function ImportDeclaration(path) {
    var _a;
    const { node: { source, specifiers }, } = path;
    if (((_a = source.extra) === null || _a === void 0 ? void 0 : _a.rawValue) === shared.options.clientModule) {
        if (specifiers.find((specifier) => {
            return (t__namespace.isImportSpecifier(specifier) &&
                t__namespace.isIdentifier(specifier.imported) &&
                specifier.imported.name === shared.options.clientFunction);
        })) {
            this.hasImportedModule = true;
        }
    }
    shared.ImportDeclaration.call(this, path);
}

const visitors = {
    ImportDeclaration,
    ObjectProperty: shared.ObjectProperty,
    StringLiteral: shared.StringLiteral,
    TemplateLiteral: shared.TemplateLiteral,
    MemberExpression: shared.MemberExpression,
};

function transform(source) {
    const ast = parser.parse(source, { sourceType: 'module' });
    const { keys, hasImportedModule } = getContext(ast);
    const taggedKeys = keys.filter(transformKey);
    if (taggedKeys.length && !hasImportedModule) {
        importClient(ast);
    }
    const code = generate__default.default(ast, {
        // concise: true,
        jsescOption: { minimal: true },
        retainLines: true,
    }).code;
    return { code, keys: taggedKeys };
}
function transformKey(item) {
    const { identifier, entity, tags, key } = item;
    if (!identifier)
        return false;
    const node = entity.node;
    if (shared.isIgnore(identifier)) {
        if (t__namespace.isTemplateLiteral(node)) {
            node.quasis[0].value.raw = key;
        }
        else {
            entity.replaceWith(t__namespace.stringLiteral(key));
        }
        return false;
    }
    const params = [t__namespace.stringLiteral(key)];
    if (t__namespace.isTemplateLiteral(node)) {
        params.push(t__namespace.arrayExpression(node.expressions));
    }
    const tagsParam = getTagsParam(tags);
    if (tagsParam) {
        params.push(tagsParam);
    }
    entity.replaceWith(t__namespace.callExpression(t__namespace.identifier(shared.options.clientFunction), params));
    return true;
}
function getTagsParam(tags) {
    if (!tags)
        return;
    const properties = [];
    for (const language in tags) {
        const tagName = tags[language];
        if (!tagName || tagName === 'default')
            continue;
        properties.push(t__namespace.objectProperty(t__namespace.stringLiteral(language), t__namespace.stringLiteral(tagName)));
    }
    if (properties.length) {
        return t__namespace.objectExpression(properties);
    }
}
function getContext(ast) {
    const context = {
        keys: [],
        hasImportedModule: false,
    };
    traverse__default.default(ast, visitors, undefined, context);
    return context;
}
function importClient(ast) {
    const identifier = t__namespace.identifier(`{ ${shared.options.clientFunction} }`);
    const importDefaultSpecifier = t__namespace.importDefaultSpecifier(identifier);
    const importDeclaration = t__namespace.importDeclaration([importDefaultSpecifier], t__namespace.stringLiteral(shared.options.clientModule));
    ast.program.body.unshift(importDeclaration);
}

exports.transform = transform;
