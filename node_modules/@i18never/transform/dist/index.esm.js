import { parse } from '@babel/parser';
import generate from '@babel/generator';
import * as t from '@babel/types';
import traverse from '@babel/traverse';
import { options, ImportDeclaration as ImportDeclaration$1, ObjectProperty, StringLiteral, TemplateLiteral, MemberExpression, isIgnore } from '@i18never/shared';

function ImportDeclaration(path) {
    var _a;
    const { node: { source, specifiers }, } = path;
    if (((_a = source.extra) === null || _a === void 0 ? void 0 : _a.rawValue) === options.clientModule) {
        if (specifiers.find((specifier) => {
            return (t.isImportSpecifier(specifier) &&
                t.isIdentifier(specifier.imported) &&
                specifier.imported.name === options.clientFunction);
        })) {
            this.hasImportedModule = true;
        }
    }
    ImportDeclaration$1.call(this, path);
}

const visitors = {
    ImportDeclaration,
    ObjectProperty,
    StringLiteral,
    TemplateLiteral,
    MemberExpression,
};

function transform(source) {
    const ast = parse(source, { sourceType: 'module' });
    const { keys, hasImportedModule } = getContext(ast);
    const taggedKeys = keys.filter(transformKey);
    if (taggedKeys.length && !hasImportedModule) {
        importClient(ast);
    }
    const code = generate(ast, {
        // concise: true,
        jsescOption: { minimal: true },
        retainLines: true,
    }).code;
    return { code, keys: taggedKeys };
}
function transformKey(item) {
    const { identifier, entity, tags, key } = item;
    if (!identifier)
        return false;
    const node = entity.node;
    if (isIgnore(identifier)) {
        if (t.isTemplateLiteral(node)) {
            node.quasis[0].value.raw = key;
        }
        else {
            entity.replaceWith(t.stringLiteral(key));
        }
        return false;
    }
    const params = [t.stringLiteral(key)];
    if (t.isTemplateLiteral(node)) {
        params.push(t.arrayExpression(node.expressions));
    }
    const tagsParam = getTagsParam(tags);
    if (tagsParam) {
        params.push(tagsParam);
    }
    entity.replaceWith(t.callExpression(t.identifier(options.clientFunction), params));
    return true;
}
function getTagsParam(tags) {
    if (!tags)
        return;
    const properties = [];
    for (const language in tags) {
        const tagName = tags[language];
        if (!tagName || tagName === 'default')
            continue;
        properties.push(t.objectProperty(t.stringLiteral(language), t.stringLiteral(tagName)));
    }
    if (properties.length) {
        return t.objectExpression(properties);
    }
}
function getContext(ast) {
    const context = {
        keys: [],
        hasImportedModule: false,
    };
    traverse(ast, visitors, undefined, context);
    return context;
}
function importClient(ast) {
    const identifier = t.identifier(`{ ${options.clientFunction} }`);
    const importDefaultSpecifier = t.importDefaultSpecifier(identifier);
    const importDeclaration = t.importDeclaration([importDefaultSpecifier], t.stringLiteral(options.clientModule));
    ast.program.body.unshift(importDeclaration);
}

export { transform };
