import { Options } from './options';
import { FilterPattern } from '@rollup/pluginutils';
import { KeyItem } from './visitors';
export type PluginOptions = Partial<Options> & {
    include?: FilterPattern;
    exclude?: FilterPattern;
    langKey?: string;
    storageType?: 'cookie' | 'localStorage' | 'sessionStorage';
};
export declare function generateScript(version: string, options: PluginOptions): string;
export declare function queryVersion(keys: KeyItem<unknown>[]): Promise<string>;
